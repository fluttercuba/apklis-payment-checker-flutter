name: apklis_payment_checker
description: Flutter plugin for Apklis payment checking.
version: 0.1.1
homepage: https://github.com/fluttercuba/apklis-payment-checker-flutter

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: cu.fluttercuba.apklis_payment_checker
        pluginClass: ApklisPaymentCheckerPlugin
